import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  container: {
    marginTop: 15,
    flexDirection: "row",
  },
  textInput: {
    backgroundColor: "#eee",
    borderRadius: 20,
    marginTop: 7,
    fontWeight: "700",
  },
  textInputContainer: {
    backgroundColor: "#eee",
    borderRadius: 50,
    flexDirection: "row",
    alignItems: "center",
  },
  rightInputButton: {
    backgroundColor: "#fff",
    borderRadius: 20,
    marginRight: 8,
    padding: 9,
    flexDirection: "row",
    alignItems: "center",
  },
});
